"""Evaluation package (AI evaluation backend v2).

Stub created by the registry work so that ``evaluation.registry`` and
``evaluation.challenge_data`` are importable; the service layer
(``init_evaluation`` and friends) is added by the backend work.
"""
